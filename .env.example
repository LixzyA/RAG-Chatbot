# Secrets and tokens only. All non-secret configuration lives in
# src/app/config.py with sane defaults (Settings(BaseSettings) loads env vars
# via SettingsConfigDict). See CLAUDE.md "Environment" section and
# src/app/config.py for the full list of available knobs.

HF_TOKEN=
JWT_SECRET_KEY=

